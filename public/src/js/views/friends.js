// Generated by CoffeeScript 1.3.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  define(["jquery", "underscore", "backbone", "text!templates/friends.html", "views/baseview", "jqueryui/slider"], function($, _, Backbone, Friends, BaseView) {
    var FriendsView;
    FriendsView = (function(_super) {

      __extends(FriendsView, _super);

      function FriendsView() {
        return FriendsView.__super__.constructor.apply(this, arguments);
      }

      FriendsView.prototype.el = $("#friends");

      FriendsView.prototype.friendsTemplate = _.template(Friends);

      FriendsView.prototype.initialize = function() {
        var $scrollContent, $scrollPane;
        this.render();
        $scrollPane = $(".scroll-pane");
        $scrollContent = $(".scroll-content");
        return this.$scrollBar = $(".scroll-bar").slider({
          slide: function(event, ui) {
            if ($scrollContent.width() > $scrollPane.width()) {
              $scrollContent.css("margin-left", Math.round);
              return ui.value / 100 * ($scrollPane.width() - $scrollContent.width()) + "px";
            } else {
              return $scrollContent.css("margin-left", 0);
            }
          }
        });
      };

      FriendsView.prototype.render = function() {
        this.$el.html(this.friendsTemplate);
        return this;
      };

      FriendsView.prototype.toggle = function() {};

      return FriendsView;

    })(BaseView);
    return FriendsView;
  });

}).call(this);
