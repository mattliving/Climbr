// Generated by CoffeeScript 1.3.3
(function() {

  define(["jquery", "underscore", "backbone", "views/baseview", "views/nav", "views/sidebar", "views/map"], function($, _, Backbone, BaseView, NavView, SidebarView, MapView) {
    var AppView;
    AppView = BaseView.extend({
      el: $("body"),
      initialize: function() {
        this.render();
        return $(window).resize(function() {
          var h, offsetTop;
          h = $(".container-fluid").height();
          offsetTop = 80;
          return $("#map").css("height", h - offsetTop);
        }).resize();
      },
      render: function() {
        var mapView, navView, sidebarView;
        navView = new NavView();
        sidebarView = new SidebarView();
        mapView = new MapView();
        return this;
      }
    });
    return AppView;
  });

}).call(this);
